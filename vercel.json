{
    "builds": [{
        "src": "edirbackend/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "edirbackend/wsgi.py"
        }
    ]
}